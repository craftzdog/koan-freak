var consts = {
  config_fn: __dirname + '/../config/app.json',
  tmp_dir: '/tmp',
  appId: 'webService',
  sessionSecretKeys: ['some secret key'],
  accessLogFileName: __dirname + '/../log/access.log'
};

module.exports = consts;
